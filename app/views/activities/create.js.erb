<% if @activity.errors.any? %>
  $('.ajax-content').html('<%= escape_javascript(render(template: 'activities/new')) %>');
  initControls();
  initTypeAhead();
<% else %>
  window.location.href = '<%= show_date_user_activities_path(@user, date: @activity.date) %>';
<% end %>
