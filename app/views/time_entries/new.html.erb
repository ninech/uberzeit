<div class="row">
  <div class="small-12 columns">
    <h2><%= t('.add_entry') %>
  </div>
</div>
<%= form_for [@time_sheet, @time_entry], remote: true do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
<% end %>
