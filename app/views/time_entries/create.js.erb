<% if @time_entry.errors.any? %>
  $('.ajax-content').html('<%= escape_javascript(render(template: 'time_entries/new')) %>');
  initControls();
<% else %>
  window.location.href = '<%= show_date_user_time_entries_path(@user, date: @time_entry.start_date) %>';
<% end %>

