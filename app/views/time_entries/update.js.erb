<% if @time_entry.errors.any? %>
  $('.ajax-content').html('<%= escape_javascript(render(template: 'time_entries/edit')) %>');
  initControls();
<% else %>
  window.location.href = '<%= show_date_time_sheet_path(@time_sheet, date: @time_entry.start_date) %>';
<% end %>
