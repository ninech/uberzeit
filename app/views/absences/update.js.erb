<% if @absence.errors.any? %>
  $('.ajax-content').html('<%= escape_javascript(render(template: 'absences/edit')) %>');
  initControls();
<% else %>
  window.location = '<%= year_user_absences_path(@user, @absence.start_date.year) %>';
<% end %>
