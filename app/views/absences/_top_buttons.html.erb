<% if can? :manage, Absence %>
  <div class="top-buttons">
    <a href="#" class="button remote-reveal" data-reveal-id="absence-modal" data-reveal-url="<%= new_user_absence_path(@user) %>">
      <%= icon(:plus) %>
      <%= t '.add_absence' %>
    </a>
  </div>
<% end %>
