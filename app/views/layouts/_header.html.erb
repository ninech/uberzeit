<div class="header container">
  <div class="row">
    <div class="large-12 columns">
      <div class="logo">
        <%= image_tag('logo.svg', size: '122x36') %>
      </div>
      <div class="navigation">
        <ul>
          <li><%= link_to t('.timesheet'), time_sheet_path(current_user.current_time_sheet) %></a></li>
          <li><%= link_to t('.absences'), time_sheet_absences_path(current_user.current_time_sheet) %></li>
          <li><%= link_to t('.reports'), work_summary_time_sheet_path(current_user.current_time_sheet, Date.current.year) %></li>
          <% if show_manage_link_in_navigation? %>
            <li><%= link_to t('.manage'), public_holidays_path %></li>
          <% end %>
        </ul>
      </div>
      <% if has_top_buttons? %>
        <%= render "#{controller.controller_name}/top_buttons" %>
      <% end %>
    </div>
  </div>
</div>
