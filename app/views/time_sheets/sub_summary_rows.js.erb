current_month = $('.sub-summary-row').data('month');
$(".sub-summary-row").remove();

if(current_month != <%=@month%>) {
  $("tr[data-month=<%=@month%>]").after("<%=escape_javascript(render(partial: 'sub_summary_rows', locals: {summary: @summary}))%>")
}
